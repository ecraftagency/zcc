# Copy to terraform.tfvars and edit. Only ssh_cidr is required.
ssh_cidr = "203.0.113.4/32" # your IP /32 — curl -s ifconfig.me
# instance_type = "c7g.16xlarge"   # 64 vCPU to halve milestone wall-clock
# seed_count    = 10000
