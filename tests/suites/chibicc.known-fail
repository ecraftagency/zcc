# chibicc baseline. 2026-08-20: builtin+typeof ĐẠT (bare `typeof` + ty_compat
# so chữ ký Func). Còn string = chuỗi UTF-16/32 C11 u"/U"/u' — HOÃN tới sau khi
# bỏ Mach-O (u"/U cần refactor bool→width qua 2 backend; bỏ Darwin xong chỉ wire
# 1 backend ELF, rẻ hơn). Userland musl không đụng u"/U → không gấp.
FAIL string
