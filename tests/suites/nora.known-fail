# nora baseline — case ORACLE-INVALID còn sót (diff tại observable AUTO-stack
# placement-incidental, KHÔNG phải bug zcc). Proof cơ học kèm theo.
#
# ── #3 chapter_15/allocation/test_alignment (Vu 2026-08-20) ──
# Test đòi `(unsigned long)int_arr % 16 == 0` cho 3 AUTO int array (stack).
# C99 6.2.8p1: _Alignof(int[N]) = _Alignof(int) = 4 — 16-align KHÔNG do ISO C đòi.
# PROOF incidental: gcc trên CÙNG mảng frame khác (thêm printf) cho %16=8 chứ
# không 0 → 16-align chỉ là placement-luck của stack layout, gcc KHÔNG guarantee
# (LOCAL_ALIGNMENT gcc chỉ boost aggregate lên 8, không 16). zcc cho ≥4-align
# (đo %4=0) = ĐÚNG C. Muốn zcc match exit-0 của gcc phải over-align auto lên 16
# — VƯỢT cả gcc, không phải parity. ⇒ oracle vô hiệu, giữ known-fail.
# (Khác #4: #4 đã PASS sau khi bơm static-align lên 8 khớp gcc — commit cùng đợt.)
FAIL chapter_15_valid_allocation_test_alignment
